{
  "name": "tokentrap-worker-template",
  "version": "1.0.1",
  "private": true,
  "description": "Cloudflare Workers backend for TokenTrap (level 1) - sessions, structured logging, canaries, rate limiting.",
  "license": "MIT",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "tokentrap-ai": "1.0.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.9",
    "wrangler": "^3.99.0"
  }
}
