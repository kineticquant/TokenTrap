// Set this to your deployed worker URL, then redeploy this folder.
TokenTrap.init({
  container: "#trap",
  persona: "Internal AI Assistant",
  apiEndpoint: null, // e.g. "https://token-trap-worker.your-name.workers.dev"
});
